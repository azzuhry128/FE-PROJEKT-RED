function FriendRequestAdapter() {
    // 1st query SEND REQUEST 
    // 2nd query POST TO notification (id notification-auto, sender auto, receiver(requestUser) request user FROM sender body, content(message))
    // after click confirm POST 

    // const {notificationState} = 
    return (
        
    )
}

export { FriendRequestAdapter }